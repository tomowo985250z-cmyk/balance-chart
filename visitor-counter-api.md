# 閲覧人数APIの接続

現在、保存先APIは未設定です。`index.html` の `visitorCounter` にある
`data-api-url=""` に公開済みAPIのHTTPS URLを設定してください。
未設定・通信失敗時は架空の人数を表示せず、「本日のお客様 —人」と表示します。

## APIに必要な実装

- GET: 日本時間（Asia/Tokyo）の当日人数を返す。加算しない。
- POST: JSON `{ "deviceId": "端末のランダムID" }` を受け取り、サーバーで
  日本時間の当日を決定する。当日と端末IDの組を一意制約で保存し、初回のみ加算する。
  重複チェック・保存・人数取得はトランザクション等で整合性を保証する。
  リトライ・同時リクエストでも同じ組を重複加算しないこと。
- GET/POSTの成功レスポンス: `{ "date": "2026-09-09", "count": 123 }`。
  初訪問がない日は0を返す。日付はサーバー側で毎回決定し、日ごとに別集計する。
- レスポンスは `Cache-Control: no-store`。別オリジンの場合はサイトのオリジンに対して
  CORSを許可し、OPTIONS / GET / POST と Content-Type ヘッダーに対応する。
- DB認証情報や管理用APIキーをHTML/JavaScriptに埋め込まない。

フロントは1分ごと、および画面復帰時に同期します。訪問済み日はAPIの加算成功後のみ
localStorageに保存します。保存領域を利用できない場合は閲覧数取得だけを行います。
端末判定は同じブラウザー・同じサイトのlocalStorage単位です。
保存データの消去や別ブラウザーの利用は別端末として扱われます。

公開先と保存サービスが決まったら、その環境で上記APIを実装・公開してURLを設定し、
別端末、当日リロード、複数タブ、日付変更、通信失敗後の再試行を実APIで確認してください。
